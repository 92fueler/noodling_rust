# Ignore build artifacts
/target/

# Optional: editor/OS junk
.DS_Store
*.swp

# Ignore everything under src
src/*

# BUT keep directories so we can un-ignore files inside them
!src/**/
# Keep all .rs files recursively
!src/**/*.rs
