/**
 * 438. find all anagrams in a string
 *
 * Given two strings s and p,
 * return an array of all the star indices fo p's anagrams in s.
 *
 * Input: s = "abab", p = "ab"
 * Output: [0,1,2]
 */

impl Solution {
    pub fn find_anagrams(s: String, p: String) -> Vec<i32> {}
}
